export type ScenarioId =
  | 'A'
  | 'B'
  | 'C'
  | 'D'
  | 'E'
  | 'F'
  | 'G'
  | 'H'
  | 'I'
  | 'J'
  | 'K'
  | 'L'
  | 'M'
  | 'N';

export interface ScenarioData {
  guide: string;
  name: string;
}

// ─── Scenario guide texts ─────────────────────────────────────────────────────

export const SCENARIO_GUIDES: Record<ScenarioId, ScenarioData> = {
  A: {
    name: '弱口令 / 暴力破解',
    guide: `
#### A.1 请求特征
- **协议/方法**：HTTP POST / SSH / FTP / RDP / Telnet / DB协议，注明具体协议
- **目标服务**：目标 IP:Port 对应的服务名称（如 SSH-22、FTP-21、HTTP-8080）
- **关键请求头**：Content-Type、User-Agent、Authorization 等

#### A.2 凭据载荷解析
逐参数列出，格式：
\`\`\`
username = admin          # 尝试的用户名
password = 123456         # 尝试的密码
\`\`\`
弱口令判断标准（命中即标注）：
- 纯数字 ≤ 8 位：如 123456、12345678
- 纯字母 ≤ 8 位：如 password、admin
- 用户名与密码相同：如 admin/admin、root/root
- 常见默认凭据：admin/admin123、guest/guest、sa/空密码
- 键盘序列：qwerty、1qaz2wsx

#### A.3 攻击研判
- **是否成功**：message 中有无"登录成功"、"认证通过"、响应码 200 等成功标志；或"失败"、"认证错误"、响应码 401/403 等失败标志
- **频次分析**：结合 times 字段或历史事件判断是否为持续暴力破解
- **SSH/RDP 成功判定**：若协议为 SSH/RDP 且无失败标志，应查询 dstIp 后续是否有异常行为事件

#### A.4 请求回放验证（存在明文凭据时必须执行）

**触发条件**：A.2 中解析出可用的 username / password，且目标 IP:Port 已知。

**执行规范**：
- 根据目标端口选择正确协议：22→ssh、21→ftp、3389→rdp、80/8080→http、443/8443→https
- 携带 A.2 解析的**实际凭据**调用 **replayRequest**
- 若报文含多组凭据尝试，优先对**最后一组**（攻击者最终尝试的）进行回放

**HTTP/HTTPS 传参策略**：

| 情形 | 做法 |
|------|------|
| message 中含明文 HTTP 报文（有请求行 + 请求头 + 请求体） | 将完整报文原样赋给 \`rawRequest\`，不需要拆分字段 |
| 报文已加密或仅有凭据片段，无完整报文 | 将 POST 数据赋给 \`body\`，GET 参数赋给 \`queryParams\`，额外请求头赋给 \`headers\` |

**结果解读**：

| authResult | 含义 | 对研判结论的影响 |
|-----------|------|----------------|
| success | 凭据有效，认证成功 | **直接升为"真实攻击成功"**，无论 message 中的原始标志如何 |
| failed | 认证被拒绝 | 倾向"真实攻击失败"；结合频次判断是否仍在持续尝试 |
| timeout | 目标不可达 / 连接超时 | 无法确认，依赖 message 原始标志及历史事件综合判断 |
| error | 协议错误 / 连接被拒绝 | 目标服务可能不存在，倾向"误报"或"真实攻击失败" |`,
  },

  B: {
    name: 'Web 应用攻击',
    guide: `
> **注**：subType=3201 CGI访问若 message 中无攻击载荷，通常为正常业务访问，应直接判定为**误报**。

#### B.1 请求特征
- **HTTP 方法**：GET / POST / PUT / DELETE / OPTIONS 等
- **目标 URL**：完整路径（含查询参数）
- **关键请求头**：User-Agent（判断是否扫描器）、Cookie、Referer、X-Forwarded-For

#### B.2 Payload 解析
逐参数还原攻击意图，按攻击子类型细化：

| 子类型 | 关注重点 |
|--------|----------|
| SQL注入（322901/2201/4301） | 注入点参数名、注入语句（UNION/SELECT/OR 1=1/sleep等）、目标数据库 |
| XSS（322902/2202/4302） | 注入的 \`<script>\` 标签或事件属性、是否有 cookie 窃取代码 |
| RCE/命令注入（322903/2207/4307） | 执行的系统命令（ls/whoami/id/cat /etc/passwd/wget/curl等） |
| Webshell上传（322905/2205/4305） | 上传文件名、文件内容特征（eval/system/exec/passthru等） |
| Webshell连接（322906/2236/4336） | 连接的 Webshell 路径、传入参数 |
| 目录遍历/文件包含（322904/2208/2209） | 遍历路径（../../../etc/passwd 等） |
| SSRF（2252/4352） | 请求的内网地址/云元数据接口（169.254.169.254等） |
| XXE（323018/4354） | XML 外部实体引用内容 |
| 反序列化（2242-2246/4342-4346/323002） | 序列化特征（rO0AB/aced0005/yv66vg等）及还原的命令 |

#### B.3 特征匹配
- 攻击载荷是否命中已知攻击模式
- 是否使用了扫描器（User-Agent 特征：sqlmap/nikto/masscan/nmap/burpsuite等）
- 响应状态码：200 可能成功；403/404/500 可能失败或被拦截`,
  },

  C: {
    name: '漏洞利用与渗透',
    guide: `
> **注**：subType=3235（不安全配置）和 subType=2402-2420（漏洞检测结果）若 message 无实际利用载荷，通常判定为**真实攻击失败**（漏洞存在但未被利用）；若有利用证据则判定为真实攻击成功。

#### C.1 漏洞识别
- 漏洞类型（RCE/反序列化/路径穿越/未授权访问等）
- 涉及产品与版本（从 message 或资产信息推断）
- 对应 CVE 编号（如 message 中有提及）

#### C.2 利用载荷分析
- 利用请求的关键特征字段
- 攻击成功的判定标志（回显/dnslog/反弹shell特征）
- 是否已有 POC 公开（根据漏洞类型推断危险程度）

#### C.3 横向移动研判
- 若 subType 为 323003（SMB/NTLM横向移动），重点分析目标内网扩散范围
- 查询 srcIp 的历史事件，判断是否已作为跳板机`,
  },

  D: {
    name: '安全扫描 / 异常扫描',
    guide: `
#### D.1 扫描分析
- **扫描类型**：端口扫描 / 主机存活 / Web路径 / 漏洞探测
- **扫描目标范围**：单 IP 还是网段（判断针对性攻击还是广撒网）
- **是否为攻击前侦察**：结合历史事件，查看扫描后是否有漏洞利用或暴力破解事件
- **扫描来源性质**：是否为已知安全工具 IP / 内部巡检系统（判断误报可能性）`,
  },

  E: {
    name: '违规行为（外联 / 代理 / 远控 / 接入 / 进程 / 配置违规）',
    guide: `
#### E.1 违规行为专项分析

| 违规子类 | 关注重点 |
|----------|----------|
| 违规外联（3199） | 外联目标 IP/域名及归属地；是否为境外 IP；传输数据量 |
| 违规代理（3102） | 使用的代理工具（SS/V2Ray/Clash/Tor）；是否绕过审计 |
| 违规远控（3198） | 工具类型（TeamViewer/向日葵/ToDesk/RustDesk）；是否为主动安装 |
| 违规穿透（3103） | 穿透工具（花生壳/FRP/Ngrok）；内网暴露的服务端口 |
| 违规接入（3104） | 接入设备是否有资产记录；是否为串网或移动终端接入 |
| 违规进程（3106） | 黑名单进程名称；是否为恶意软件或未经审批工具 |
| 资产配置违规（3108） | 多网卡/多IP是否为多穴主机；弱加密协议（TLS1.0/RC4）影响范围 |
| KVM/IPMI违规访问（3101） | 带外管理端口（623/UDP）访问是否经授权；操作内容 |
| 飞行违规（3105） | 违规飞行的地理位置；是否在禁飞区范围内；是否有 GPS 轨迹/视频记录佐证 |
| 一机两用（3206） | 设备同时连接两个不同安全域的证据；违规设备 IP/MAC；是否已被系统阻断（3206BL） |
| 视频网合规（VULN_COMPLIANCE_SUB） | 非法接入设备指纹；违规外联目标；无线路由风险评估 |

- 是否为主动配置还是被植入后门（结合历史事件判断）
- 内网主机是否已有资产记录（判断是否为已知/授权设备）`,
  },

  F: {
    name: '恶意程序 / C2 通信 / 后门 / 僵尸网络',
    guide: `
#### F.1 恶意行为专项分析

| 子类 | 关注重点 |
|------|----------|
| 勒索/挖矿（323101/323102） | 勒索家族特征（.locked/.encrypt后缀/赎金说明）；挖矿进程（xmrig/minerd）及 CPU/GPU 占用 |
| DGA/C2心跳（323103/323104） | DGA域名随机性特征；心跳周期、包大小、加密方式（JA3/HASSH指纹） |
| 恶意文件传播（323105） | 传播文件名/Hash；传播方向（内→外 还是 外→内 还是 内→内） |
| 后门投递（323701） | 投递方式（邮件附件/Web漏洞/供应链）；Payload 类型（Shellcode/脚本/EXE） |
| Web应用后门（323702） | Webshell 路径；连接特征（菜刀/蚁剑/冰蝎/哥斯拉特征请求头/加密方式） |
| 网络设备/物联网后门（323705/323706） | 固件版本；是否为已知后门 CVE |
| 恶意通信（323901/323902） | 目标 IP/域名威胁情报命中；通信频次；是否为 DGA 生成域名 |
| 钓鱼邮件（323301） | 邮件发件人域名仿冒特征；附件/链接内容；目标用户身份 |
| SMTP/POP3异常（323303） | 异常账号/认证失败频次；是否为账号爆破或数据外传 |

- 受感染主机是否已成为内网攻击跳板（查询 srcIp 的 ipRole="src" 历史事件）
- C2 服务器 IP/域名威胁情报（to_ids=true 为高置信度 IOC）`,
  },

  G: {
    name: 'DoS / DDoS 攻击',
    guide: `
#### G.1 拒绝服务分析
- **攻击类型**：SYN Flood / UDP Flood / HTTP Flood / 反射放大等
- **攻击流量规模**：pps/bps（从 message 或告警详情中提取）
- **目标服务影响**：响应超时 / 服务中断 / 带宽耗尽
- **是否为分布式来源**：多 srcIp 同时攻击同一 dstIp（查询 dstIp 的历史事件确认）`,
  },

  H: {
    name: '欺骗劫持',
    guide: `
#### H.1 欺骗劫持分析

| 子类型 | 分析要点 |
|--------|----------|
| ARP欺骗（321701） | 伪造的 MAC-IP 映射关系；影响的内网范围；是否导致流量被中间人截获 |
| LLMNR/NBT-NS（321702） | 是否用于窃取 NTLM 哈希（Responder 攻击特征）；受害主机数量 |
| DNS欺骗（321703/321704） | 被篡改的域名及指向的恶意 IP；受影响的客户端范围 |
| DHCP欺骗（321705） | 伪造的 DHCP 服务器地址；分配的恶意网关/DNS |`,
  },

  I: {
    name: '用户行为异常',
    guide: `
#### I.1 行为异常专项分析

| 子类型 | 分析要点 |
|--------|----------|
| 3301/330901 异地登录 | 登录来源地与常用地差异；是否在异常时区 |
| 3302/330904 同人不同终端 | 同一账号在多设备同时在线；是否合理（不同班次？） |
| 3303 红名单 | 触发的红名单人员身份；访问内容 |
| 3304/330906 退休用户访问 | 账号是否应已注销；访问的资源敏感级别 |
| 3306 频繁操作 | 操作频次是否超基线；操作对象是否集中于敏感资源 |
| 3307/330902 非工作时间访问 | 访问时段（凌晨/节假日）；操作内容 |
| 3309 认证登录异常 | 综合研判：异地+非工作时间+高频失败→账号被盗高风险 |
| 3310 PKI证书异常 | 证书编号；是否跨区域/多机使用；是否已离退休 |
| 3311 数据查询行为异常 | 查询量/频次/对象离散度；是否符合岗位职能 |
| 3312 终端操作行为 | 截屏/录屏/大量拷贝→数据窃取风险；非工作时间高频操作 |
| 3227 离退休未注销 | 该账号最后一次登录时间；是否有数据操作记录 |

- 是否与同期安全事件（暴力破解/数据查询异常）关联，综合判断内鬼/账号被盗/正常误触发`,
  },

  J: {
    name: 'APT 事件',
    guide: `
#### J.1 APT 事件分析
- **APT 组织**：关联的 APT 组织名称及其惯用 TTP（战术/技术/过程）
- **攻击链条**：C2 通信、鱼叉邮件、横向移动等多阶段攻击链条还原
- **上报要求**：APT 事件**必须上报至上级安全部门**，不得单独处置
- **证据保全**：保留完整的网络流量、日志、内存镜像，勿自行清除`,
  },

  K: {
    name: '数据安全事件',
    guide: `
#### K.1 数据安全专项分析

| subType | 分析要点 |
|---------|----------|
| 3401-3404（信息泄露风险：身份证/手机号/车牌/用户信息） | 泄露数量规模；是否批量导出；涉及数据的敏感等级 |
| 3405（身份证号批量查询/导出） | 查询条数；操作账号身份；是否为业务正常操作 |
| 3406（敏感人员被查） | 被查询的警员/红名单人员；查询人身份及部门；是否跨部门越权 |
| 3407（轨迹数据异常访问） | 查询的地理范围；是否跨区域；数据量是否超基线 |
| 3408/3409（数据库备份/源代码外传） | 传输目标 IP/域名；文件类型和大小；是否为非工作时间 |
| 3410（敏感关键词内容传输） | 触发的关键词；内容载体（邮件/文件/HTTP） |
| 3411/3412（退休人员/非授权部门访问高密数据） | 账号状态；访问的数据级别；是否符合业务授权 |

**结论判定核心**：以**数据是否实际流出**为准——流出为"真实攻击成功"，被拦截为"真实攻击失败"，内部合规操作触发规则为"误报"`,
  },

  L: {
    name: '设备健康 / 物联网 / 无人机安全',
    guide: `
#### L.1 设备健康专项分析

| subType | 分析要点 |
|---------|----------|
| 350101（低电量告警） | 设备是否为关键监控节点；是否影响业务连续性 |
| 350102（设备离线/心跳丢失） | 离线是否导致监控盲区；是否为计划内维护 |
| 350201-350210（无人机传感器/信号/降落异常） | 异常发生时段；是否在执行任务中；是否有外部干扰信号迹象（GPS漂移→可疑欺骗） |
| v_importance_offline_other（重要设备离线） | 离线设备名称；是否为计划内维护；持续时长 |
| v_env_check_other（系统环境变更） | 变更内容（新增进程/配置修改）；是否有对应变更工单 |

- 此类事件通常研判为"误报"（设备故障）或"真实攻击成功"（信号干扰/固件后门），需结合时间窗口内的其他安全事件综合判断`,
  },

  M: {
    name: '互联网安全事件',
    guide: `
#### M.1 互联网网事件研判

| subType | 名称 | 研判要点 |
|---------|------|----------|
| 4101 | 越权操作 | 操作账号是否有该资源权限；操作内容是否涉及敏感数据 |
| 4102 | 攻击行为 | 攻击类型和目标，结合 message 匹配场景 B/C 的分析方法 |
| 4103 | 切换账号风险 | 原账号与切换目标账号的权限差异；是否为提权行为 |
| 4104 | 新增用户账户 | 是否为计划内账号创建；新账号权限是否异常（高权限） |
| 4105 | SSH提醒行为 | 登录来源 IP；登录账号；是否为非常用来源或非工作时间 |
| 4106 | 未授权操作 | 操作的资源路径；账号角色；操作是否被系统拒绝 |
| 4107 | 试探攻击 | 探测的端口/路径；是否为扫描工具特征；是否为后续攻击前侦察 |

> **type=42（正常事件：4201系统运行行为/4202合规运维操作/4203系统提醒）**：直接判定为**误报**，说明触发规则的业务行为即可，无需深度分析。`,
  },

  N: {
    name: '其他',
    guide: `
#### N.1 研判规则

| subType | 名称 | 研判规则 |
|---------|------|----------|
| 3209 | 访问控制 | 分析被拦截的访问行为是否符合权限策略；通常为**真实攻击失败** |
| 3215 | 可疑行为 | 结合 message 中的行为特征，按最相近场景（A-M）补充分析；无明显攻击意图则判**误报** |
| 3222 | 网络通讯 | 仅记录通讯行为，无攻击意图，通常判**误报**；若通信目标命中威胁情报则升级至场景 F |
| 3224 | 协议分析 | 协议合规性检测结果；无异常流量则为**误报**；存在协议异常特征则结合场景 G 判断 |
| 3226 | HTTP出错页面 | Web服务器返回错误页面（404/500等），通常为**误报**（探测类攻击已失败） |
| 3240 | 协议异常 | 协议格式违规/畸形包；若为单次偶发通常为**误报**；持续高频则结合场景 G 判断 |
| 4401 | IPS防护 | IPS 已主动拦截，几乎固定判定为**真实攻击失败**；确认拦截规则有效并关注后续重试 |
| v_info_collect_other | 信息收集（其他） | 探测行为；结合场景 D 分析；单次探测通常为**真实攻击失败** |
| v_malicious_botnet_other | 恶意僵尸网络（其他） | 结合场景 F 分析；优先查询威胁情报（to_ids=true 为高置信度） |
| v_other_other | 其他（视频网） | 按 message 内容判断；无明确攻击特征则判**误报** |
| 32XX/32XX01 | 其他安全事件 | 按 message 内容匹配最相近场景；无法匹配则参考场景 C 分析 |`,
  },
};

// ─── Scenario detection ───────────────────────────────────────────────────────

function toNum(s: string): number {
  const n = Number.parseInt(s, 10);
  return Number.isNaN(n) ? -1 : n;
}

function inRange(s: string, min: number, max: number): boolean {
  const n = toNum(s);
  return n >= min && n <= max;
}

export function detectScenario(
  type: number | undefined,
  subType: string | undefined,
  eventName: string | undefined,
): ScenarioId {
  const st = subType ?? '';
  const name = eventName ?? '';
  const t = type ?? 0;

  // Type-based priority (most authoritative)
  if (t === 33) return 'I';
  if (t === 34) return 'K';
  if (t === 35) return 'L';
  if (t === 41 || t === 42) return 'M';

  // Scenario A: Weak password / Brute force
  if (
    /^3207(?:0[1-7])?$/.test(st) ||
    ['3208', '3228', '2218', '2222', '2223', '2401', '4318', '4322', '4323'].includes(st) ||
    ['v_3001', 'v_3003', 'v_3006', 'v_3007'].includes(st) ||
    ['v_1001', 'v_1003', 'v_1005', 'v_1006', 'v_1007'].includes(st) ||
    ['v_2001', 'v_2003', 'v_2005', 'v_2006', 'v_4001', 'v_4002'].includes(st) ||
    /弱口令|暴力破解|口令|高频失败|登录失败/.test(name)
  )
    return 'A';

  // Scenario B: Web application attacks
  if (
    ['3201', '3202'].includes(st) ||
    /^3229\d{2}$/.test(st) ||
    inRange(st, 2201, 2253) ||
    inRange(st, 4301, 4356) ||
    /^32F[0-6]$/i.test(st)
  )
    return 'B';

  // Scenario C: Exploit / Penetration
  if (
    ['3203', '3211', '3219', '3220', '3221'].includes(st) ||
    /^3230\d{2}$/.test(st) ||
    /^3235(?:0[1-4])?$/.test(st) ||
    inRange(st, 2101, 2126) ||
    inRange(st, 2402, 2420) ||
    /CVE-\d{4}-\d+/.test(name)
  )
    return 'C';

  // Scenario D: Scanning
  if (/^3205\d{2}$/.test(st) || /^3236\d{2}$/.test(st) || st === '320506') return 'D';

  // Scenario E: Compliance violations
  if (
    /^3199(?:0[1-9])?$/.test(st) ||
    /^3102(?:0[1-6])?$/.test(st) ||
    /^3198(?:0[1-9]|10)?$/.test(st) ||
    /^3103(?:0[1-3])?$/.test(st) ||
    /^3104(?:0[1-8])?$/.test(st) ||
    ['3109', '3106', '310601', '3101', '310101'].includes(st) ||
    /^3108(?:0[3-6])?$/.test(st) ||
    /^3105(?:0[1-3])?$/.test(st) ||
    /^3206(?:BL|FI|FL|GR|IF|01)?$/i.test(st) ||
    ['v_illegal_in', 'v_internal_scan', 'v_multi_host', 'v_wifi_route_check'].includes(st)
  )
    return 'E';

  // Scenario F: Malware / C2
  if (
    /^3231\d{2}$/.test(st) ||
    ['3213', '3216', '3218', '3212'].includes(st) ||
    inRange(st, 2301, 2307) ||
    /^3232\d{2}$/.test(st) ||
    /^3237\d{2}$/.test(st) ||
    /^3239\d{2}$/.test(st) ||
    /^3233\d{2}$/.test(st)
  )
    return 'F';

  // Scenario G: DoS/DDoS
  if (['3210', '3214'].includes(st) || /^3225\d{2}$/.test(st) || /^3234\d{2}$/.test(st)) return 'G';

  // Scenario H: Spoofing/Hijacking
  if (/^3217\d{2}$/.test(st)) return 'H';

  // Scenario I: User behavior (subType-based)
  if (st === '3227' || inRange(st, 3301, 3312) || /^(?:3309|3310|3311|3312)\d+$/.test(st))
    return 'I';

  // Scenario J: APT
  if (/^3238\d{2}$/.test(st)) return 'J';

  // Scenario K: Data security (subType-based)
  if (inRange(st, 3401, 3412)) return 'K';

  // Scenario L: Device health (subType-based)
  if (
    ['3501', '3502'].includes(st) ||
    /^3501\d{2}$/.test(st) ||
    /^3502\d{2}$/.test(st) ||
    ['v_importance_offline_other', 'v_env_check_other', 'v_VMware_check_other'].includes(st)
  )
    return 'L';

  return 'N';
}
